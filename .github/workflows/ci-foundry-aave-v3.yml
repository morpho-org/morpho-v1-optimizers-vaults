name: Solidity tests on Morpho Aave V3

on:
  push:
    branches:
      - main
    paths:
      - "lib/**"
      - "contracts/*"
      - "contracts/aave-v3/**"
      - "test/aave-v3/**"
      - "*.lock"
      - "Makefile"
      - "remappings.txt"
      - ".github/workflows/ci-foundry-aave-v3.yml"
  pull_request:
    paths:
      - "lib/**"
      - "contracts/*"
      - "contracts/aave-v3/**"
      - "test/aave-v3/**"
      - "*.lock"
      - "Makefile"
      - "remappings.txt"
      - ".github/workflows/ci-foundry-aave-v3.yml"

jobs:
  morpho-aave-v3:
    if: "! github.event.pull_request.draft"
    name: Morpho-Aave V3
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
          ssh-key: ${{ secrets.CI_PRIVATE_KEY }}

      - uses: actions/setup-node@v3
        with:
          node-version: "16"

      - name: Node dependencies cache
        uses: actions/cache@v3
        with:
          path: "**/node_modules"
          key: yarn-${{ hashFiles('**/yarn.lock') }}

      - name: Install dependencies
        run: yarn install

      - name: Install Foundry
        uses: onbjerg/foundry-toolchain@v1
        with:
          version: nightly

      - name: Run tests
        run: make test PROTOCOL=aave-v3 NETWORK=avalanche-mainnet
        env:
          ALCHEMY_KEY: ${{ secrets.ALCHEMY_KEY }}
